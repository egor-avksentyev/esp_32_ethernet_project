#pragma once

// ============================================================================
// wifi_setup.h/.cpp — подключение к Wi-Fi и переподключение при потере связи.
// Событийное (WiFi.onEvent), не поллинг — состояние "подключено" узнаём сразу из
// ARDUINO_EVENT_WIFI_STA_GOT_IP/_DISCONNECTED, а не гадаем раз в N секунд. При разрыве —
// переподключение с нарастающей паузой (см. WIFI_RECONNECT_BASE_MS/_MAX_MS, config.h),
// сброс паузы к минимуму сразу после успешного реконнекта. Способ ввода SSID/пароля
// (зашитые значения сейчас, captive portal в будущем) этого модуля не касается,
// см. wifi_credentials.h — что бы ни изменилось в способе ввода, реконнект-логика та же.
// ============================================================================

#include <Arduino.h>

void wifiSetupBegin();

// Вызывать из loop() каждую итерацию — не блокирует; сама решает, пора ли пробовать
// переподключиться (см. backoff выше)
void wifiSetupMaintain();

bool wifiIsConnected();
