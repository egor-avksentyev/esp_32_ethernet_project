#pragma once

// Скопируй этот файл в wifi_credentials.h (тот уже в .gitignore — настоящий пароль
// в репозиторий не попадёт) и подставь свою сеть.
//
// Решение по способу ввода пароля (captive portal при первом запуске и т.п.) пока
// осознанно отложено — см. README.md, "Wi-Fi credentials". Сейчас — просто зашитые
// значения; при переезде на новую точку доступа нужно поправить их здесь и перезалить.

#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"
